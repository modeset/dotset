#!/bin/bash

if [[ "$1" == "-h" || "$1" == "--help" || "$1" == "help" ]]; then cat <<HELP
Search the jQuery API Docs:

Usage: $(basename "$0") [term]
Example: $(basename "$0") animation
HELP
exit; fi

open "http://api.jquery.com/?s=$*"

