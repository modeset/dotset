#!/bin/bash

if [[ "$1" == "-h" || "$1" == "--help" || "$1" == "help" ]]; then cat <<HELP
Open up the results of a google search:

Usage: $(basename "$0") [term]
Example: $(basename "$0") ron burgandy
HELP
exit; fi

open "http://www.google.com/search?hl=en&q=$*"

