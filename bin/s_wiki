#!/bin/bash

if [[ "$1" == "-h" || "$1" == "--help" || "$1" == "help" ]]; then cat <<HELP
Search Wikipedia:

Usage: $(basename "$0") [term]
Example: $(basename "$0") mansfield
HELP
exit; fi

open "http://en.wikipedia.org/wiki/Special:Search?search=$*&go=Go"

