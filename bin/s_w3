#!/bin/bash

if [[ "$1" == "-h" || "$1" == "--help" || "$1" == "help" ]]; then cat <<HELP
Search the W3 Schools API Docs:

Usage: $(basename "$0") [term]
Example: $(basename "$0") background
HELP
exit; fi

open "http://www.google.com/search?sitesearch=www.w3schools.com&as_q=$*"

